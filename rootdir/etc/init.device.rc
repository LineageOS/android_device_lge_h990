# Extras for the H990* variants of the LGE V20

# Mount the extra flash device
on fs
	wait /dev/block/bootdevice
	mount_all fstab.h990

# Fixup of the modem
on post-fs
	# Required for correct modem operation on H990* (otherwise FW panics)
	/system/bin/fix-h990-modem -b
